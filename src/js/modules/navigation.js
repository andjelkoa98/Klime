/**
 * navigation.js — header and mobile menu behavior.
 *
 * Scope (implemented in Phase 5/7 when the menu markup exists):
 *  - mobile menu open/close, background scroll lock, focus return to trigger
 *  - close on Escape, on link click, and automatically at ≥1024px
 *  - aria-expanded state on the trigger
 *  - header background change after ~80px of scroll (Phase 6)
 */

export function initNavigation() {
  // Phase 4: no interactive navigation markup yet — intentionally a no-op.
}
